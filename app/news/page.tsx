"use client"

import { useMemo, useState, type ReactNode } from "react"
import Link from "next/link"
import { ArrowRight, Bell, CalendarDays, Newspaper, Search, Megaphone, Sparkles } from "lucide-react"
import { Navbar } from "@/components/navbar"
import { Footer } from "@/components/footer"
import { useScrollAnimation } from "@/hooks/use-scroll-animation"

const categories = ["All", "News", "Announcements", "Events"] as const
const cards = [
  { type: "News", title: "Latest News", text: "Verified stories and institutional updates from Sammena.", icon: Newspaper },
  { type: "Announcements", title: "Announcements", text: "Important notices for the school community.", icon: Bell },
  { type: "Events", title: "Events", text: "Confirmed activities and key school dates.", icon: CalendarDays },
  { type: "News", title: "Achievements", text: "Learner and school achievements published when confirmed.", icon: Megaphone },
]
function Reveal({ children, delay = 0 }: { children: ReactNode; delay?: number }) { const { ref, isVisible } = useScrollAnimation(); return <div ref={ref} data-motion="section" data-visible={isVisible ? "true" : "false"} style={{ transitionDelay: `${delay}ms` }}>{children}</div> }

export default function NewsPage() {
  const [category, setCategory] = useState<(typeof categories)[number]>("All")
  const visible = useMemo(() => category === "All" ? cards : cards.filter(card => card.type === category), [category])
  return <main className="min-h-screen overflow-x-hidden bg-background"><Navbar/><section className="relative overflow-hidden bg-school-dark pb-20 pt-36 text-white"><div className="absolute -left-24 top-0 h-80 w-80 rounded-full bg-school-gold/10 blur-3xl"/><Reveal><div className="relative mx-auto max-w-7xl px-5 sm:px-6 lg:px-8"><p className="text-sm font-bold uppercase tracking-[0.2em] text-school-gold">News & Events</p><h1 className="mt-3 text-4xl font-bold tracking-tight md:text-6xl">Sammena Updates</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-white/65">A central institutional hub for verified school news, official announcements, achievements and confirmed events.</p><div className="mt-8 flex flex-wrap gap-3"><Link href="/search" className="inline-flex items-center gap-2 rounded-xl border border-white/15 bg-white/10 px-5 py-3 text-sm font-bold backdrop-blur transition hover:bg-white/15"><Search className="h-4 w-4"/> Search updates</Link><Link href="/calendar" className="inline-flex items-center gap-2 rounded-xl bg-school-gold px-5 py-3 text-sm font-bold text-school-dark transition hover:-translate-y-0.5"><CalendarDays className="h-4 w-4"/> View calendar</Link></div></div></Reveal></section><section className="border-b border-border bg-card"><div className="mx-auto flex max-w-7xl flex-wrap gap-2 px-5 py-5 sm:px-6 lg:px-8">{categories.map(item=><button key={item} type="button" onClick={()=>setCategory(item)} aria-pressed={category===item} className={`rounded-full px-4 py-2 text-xs font-bold transition ${category===item ? "bg-school-dark text-white shadow-sm" : "bg-school-neutral text-foreground hover:bg-muted"}`}>{item}</button>)}</div></section><section className="py-16 sm:py-20 lg:py-24"><div className="mx-auto max-w-7xl px-5 sm:px-6 lg:px-8"><Reveal><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-4">{visible.map(({icon:Icon,title,text,type},i)=><article key={title} data-motion="card" className="group rounded-3xl border border-border bg-card p-7 shadow-sm transition hover:-translate-y-1 hover:border-school-gold/40 hover:shadow-xl"><div className="flex items-center justify-between"><span className="flex h-12 w-12 items-center justify-center rounded-xl bg-school-gold/10 text-school-gold transition group-hover:bg-school-gold group-hover:text-school-dark"><Icon className="h-6 w-6"/></span><span className="text-[10px] font-bold uppercase tracking-[0.18em] text-muted-foreground">{type}</span></div><h2 className="mt-6 text-xl font-bold">{title}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{text}</p><span className="mt-6 inline-flex items-center gap-2 text-sm font-bold text-school-gold">Explore <ArrowRight className="h-4 w-4 transition group-hover:translate-x-1"/></span></article>)}</div></Reveal><Reveal delay={120}><div className="mt-10 overflow-hidden rounded-3xl border border-school-gold/20 bg-school-neutral p-8 md:p-10"><div className="flex flex-col gap-6 md:flex-row md:items-center"><div className="flex h-14 w-14 shrink-0 items-center justify-center rounded-2xl bg-school-gold/10 text-school-gold"><Sparkles className="h-7 w-7"/></div><div className="flex-1"><p className="text-xs font-bold uppercase tracking-[0.2em] text-school-gold">Publishing standard</p><h2 className="mt-2 text-2xl font-bold">Verified information first.</h2><p className="mt-2 max-w-2xl text-sm leading-7 text-muted-foreground">This public hub intentionally avoids invented or unconfirmed stories. Official updates can be added through the publishing system when approved.</p></div><Link href="/contact" className="inline-flex shrink-0 items-center justify-center gap-2 rounded-xl bg-school-dark px-5 py-3 text-sm font-bold text-white">Contact Sammena <ArrowRight className="h-4 w-4"/></Link></div></div></Reveal></div></section><Footer/></main>
}
