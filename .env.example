# Admissions persistence
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/sammena"

# 32-byte key encoded as 64 hexadecimal characters or base64.
# Generate a production secret with a cryptographically secure secret manager.
ADMISSIONS_PII_ENCRYPTION_KEY=""
